# Example local env file for development. Copy this to `.env.local` and fill values.
# `.env.local` is ignored by git (see `.gitignore`) so it's safe to store secrets there for local dev.

# Required: Azure OpenAI API key for the GPT-5 deployment. Keep this secret.
AZURE_OPENAI_KEY="set-me"

# Optional: override the API endpoint (defaults to the hackathon shared endpoint)
AZURE_OPENAI_ENDPOINT="https://psacodesprint2025.azure-api.net/gpt-5-mini/openai"

# Optional: override the deployment name (defaults to gpt-5-mini)
AZURE_OPENAI_DEPLOYMENT="gpt-5-mini"

# Optional: override the API version (defaults to 2025-01-01-preview)
AZURE_OPENAI_API_VERSION="2025-01-01-preview"

# Example DB connection string (optional for demo data)
#DATABASE_URL="mysql://root:mysecretpassword@localhost:3306/local"

# Notes:
# - Create a file named `.env.local` in the repo root with real values.
# - Restart the dev server after changing environment variables.
# - On CI (GitHub Actions) store AZURE_OPENAI_KEY as a secret and inject it into the job environment.
